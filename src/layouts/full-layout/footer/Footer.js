import { Box, Typography } from '@mui/material';

const Footer = () => (
  <Box sx={{ p: 3, textAlign: 'center' }}>
    <Typography color={'white'}>© 2024 </Typography>
  </Box>
);

export default Footer;
